# تشغيل سنتر التفوق بدون إنترنت

النظام يعمل من كمبيوتر السنتر كسيرفر محلي، ويحفظ كل البيانات في `data/center.sqlite`. لا يحتاج إلى الإنترنت أثناء الاستخدام. ويمكن لأي موبايل أو كمبيوتر على نفس شبكة الـWi-Fi فتح نفس البيانات من خلال عنوان الشبكة الذي يظهر عند تشغيل السيرفر.

## الإعداد لأول مرة

1. ثبّت Node.js 22.13 أو أحدث على كمبيوتر السنتر.
2. افتح PowerShell داخل مجلد المشروع ونفّذ:

   ```powershell
   Set-ExecutionPolicy -Scope Process Bypass
   .\setup-local-center.ps1
   ```

3. اكتب اسم مستخدم الإدارة وPIN من 4 أرقام. الـPIN لا يُحفظ في ملف إعدادات.
4. شغّل `start-local-center.cmd` واترك النافذة مفتوحة طوال فترة العمل.
5. افتح `http://localhost:3000` على كمبيوتر السنتر. للأجهزة الأخرى استخدم عنوان `LAN access` الظاهر في نافذة السيرفر، مثل `http://192.168.1.10:3000`.

إذا منع Windows الأجهزة الأخرى من الاتصال، اسمح لتطبيق Node.js على الشبكات الخاصة عند ظهور رسالة Windows Firewall. استخدم شبكة خاصة موثوقة؛ تسجيل دخول الإدارة يمر داخل شبكة السنتر المحلية عبر HTTP.

## استيراد البيانات القديمة بأمان

افحص ملف الاسترجاع أولاً، بدون تعديل SQLite:

```powershell
npm run local:import -- "C:\path\to\recovery.json" --inspect
```

تعرض الأداة كل النسخ الموجودة داخل LocalStorage وIndexedDB وتختار النسخة الأحدث حسب `savedAt`. إذا كانت قاعدة SQLite فارغة، استورد الملف:

```powershell
npm run local:import -- "C:\path\to\recovery.json"
```

إذا كانت SQLite تحتوي بيانات بالفعل فلن تستبدلها الأداة تلقائيًا. بعد مراجعة النتائج فقط، استخدم:

```powershell
npm run local:import -- "C:\path\to\recovery.json" --replace
```

قبل أي استيراد فعلي تُنشأ نسخة SQLite كاملة داخل `data/backups`، كما يحتفظ النظام بكل إصدار سابق داخل جدول التاريخ.

لنسخ Supabase مرة واحدة إلى SQLite توجد أداة قراءة فقط لا تكتب أي شيء إلى Supabase:

```powershell
npm run local:import-supabase
```

لا تشغّلها قبل ضبط مفاتيح Supabase ومراجعة النسخة المطلوبة.

الاستيراد من Supabase ينسخ اسم حساب الإدارة فقط، ولا ينسخ كلمة المرور أو الـPIN أو hash الخاص بها. عيّن PIN محليًا جديدًا باستخدام `setup-local-center.ps1` قبل فتح الإدارة.

## النسخ الاحتياطي والاسترجاع

- ينشئ السيرفر نسخة SQLite تلقائية مرة كل يوم عند تشغيله.
- من الإدارة ← الإعدادات يمكن إنشاء نسخة SQLite يدويًا أو تنزيل JSON.
- انسخ مجلد `data/backups` دوريًا إلى فلاشة أو قرص آخر. وجوده على نفس القرص يحمي من الخطأ البشري، لكنه لا يحمي من تلف القرص.
- لا تنسخ ملف `center.sqlite` يدويًا أثناء تشغيل السيرفر؛ استخدم زر النسخة الاحتياطية كي تكون النسخة متسقة.

## نقل النسخة الجاهزة إلى جهاز السنتر

1. على جهاز الترحيل الذي يحتوي البيانات المحلية، شغّل:

   ```powershell
   npm run local:backup
   ```

2. انسخ أحدث ملف `data/backups/*-manual-transfer.sqlite` إلى فلاشة. هذه نسخة متسقة حتى مع استخدام SQLite WAL؛ لا تنسخ `center.sqlite` منفردًا.
3. على جهاز السنتر، نزّل المشروع من GitHub أو انسخه بدون `node_modules` وبدون `.env`، ثم أنشئ المجلد `data` داخل المشروع.
4. انسخ ملف النسخة الاحتياطية إلى `data/center.sqlite`.
5. شغّل `setup-local-center.ps1` لتثبيت الحزم وبناء الواجهة وتعيين PIN الإدارة المحلي.
6. شغّل `start-local-center.cmd`، وافتح `http://localhost:3000`، ثم جرّب تسجيل طالب أو إنشاء حصة تجريبية قبل بدء العمل الفعلي.

## قواعد التشغيل

- لا تشغّل نسختين من السيرفر على نفس ملف SQLite.
- كل الأجهزة يجب أن تفتح عنوان كمبيوتر السنتر نفسه، لا نسخة مستضافة على الإنترنت.
- لا تنسخ ملف `.env` إلى جهاز السنتر؛ مفاتيح Supabase مطلوبة على جهاز الترحيل فقط، وليست مطلوبة لتشغيل SQLite محليًا.
- عند توقف شبكة Wi-Fi، يظل كمبيوتر السنتر نفسه يعمل عبر `localhost`. الأجهزة الأخرى تعود للمزامنة عند رجوع اتصالها بالسيرفر المحلي، والواجهة تحتفظ بطابور تغييرات في IndexedDB كنسخة طوارئ.
- إذا عدّل جهازان نفس الجزء في الوقت نفسه، يعرض النظام شاشة تعارض ولا يحذف نسخة أي جهاز تلقائيًا.
